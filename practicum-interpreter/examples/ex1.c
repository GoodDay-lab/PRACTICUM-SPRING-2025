program {
    int a = 123;
    string b = "hello";
    int d = a * 2;
    int c = a + d;
    real g = -24.4;
    int i = 0;

    write(g);
    write("here");
    read(g);
    write(g, i);

    a = a % 15;
    write(a);

    while ((1) or (i = i - 1)) {
        write(i);

        break;
    }
}
